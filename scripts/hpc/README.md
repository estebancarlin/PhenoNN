# HPC Environment Snippets

Machine-specific environment setup retained for reproducibility. These files
are examples, not portable launch scripts and not part of the installed CLI.

- `modules_LEONARDO.env`: CINECA Leonardo module setup used by an earlier run.
