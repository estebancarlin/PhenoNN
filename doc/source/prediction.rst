Prediction
==========

Using Pre-trained Models
------------------------

PhenoNN provides pre-trained models for all plant functional types. To run predictions:

.. code-block:: bash

   phenonn predict GR 4 ./example

Output Format
-------------

Predictions are saved as CSV files with 365 rows (one per day) and columns for each model.
